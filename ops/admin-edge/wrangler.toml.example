name = "webring-admin-edge"
main = "worker.js"
compatibility_date = "2026-09-14"

routes = [
  { pattern = "admin.chinazhangsan.us.ci", custom_domain = true }
]

[vars]
API_ORIGIN = "https://nav.chinazhangsan.us.ci"

# 通过 `wrangler secret put FRONTEND_PROXY_SECRET` 注入；不得提交真实密钥。
