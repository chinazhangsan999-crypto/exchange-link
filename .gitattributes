# 数据库作为独立备份资产保留在仓库历史中，但绝不能进入应用发布包覆盖线上运行库。
webring.db export-ignore
*.db-shm export-ignore
*.db-wal export-ignore
.playwright-cli/ export-ignore

# Linux 运维文件必须保持 LF，否则 systemd / bash 会把 CR 当作参数内容。
*.sh text eol=lf
*.service text eol=lf
*.timer text eol=lf
*.caddy text eol=lf
ops/Caddyfile text eol=lf
ops/webring-cloudflare-ip-sync text eol=lf
